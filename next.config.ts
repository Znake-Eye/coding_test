import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['asset.asiasport.com'], // Allow all images from this domain
  },
};

export default nextConfig;
